--- backend/ext/_core_originals/search_nlp_models.py.original	2026-05-02 17:00:39
+++ backend/onyx/natural_language_processing/search_nlp_models.py	2026-05-02 16:56:25
@@ -185,7 +185,8 @@
 
 
 def clean_model_name(model_str: str) -> str:
-    return model_str.replace("/", "_").replace("-", "_").replace(".", "_")
+    # VÖB fix: .lower() for OpenSearch index name compatibility (must be lowercase)
+    return model_str.replace("/", "_").replace("-", "_").replace(".", "_").lower()
 
 
 def build_model_server_url(
