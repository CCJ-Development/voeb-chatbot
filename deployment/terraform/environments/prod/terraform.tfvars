# Project ID wird per CLI oder .auto.tfvars uebergeben:
#   terraform plan -var="project_id=<UUID>"
#   terraform apply -var="project_id=<UUID>"
# Oder: prod.auto.tfvars (gitignored) mit project_id = "..."
